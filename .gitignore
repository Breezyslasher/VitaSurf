# Build output
build/
build-*/
out/
*.o
*.a
*.elf
*.velf
*.self
*.vpk
eboot.bin
param.sfo

# NetSurf configuration copied into the submodule by the build scripts
deps/netsurf/Makefile.config

# Editors
*.swp
*~
.vscode/
.idea/

# Vita crash dumps and logs
*.psp2dmp
psp2core-*
*.log

# Transient copies the test harnesses make next to a test or a saved page,
# so the relative paths to its resources still resolve. Deleted after each
# run; listed here so an interrupted run leaves nothing to commit.
*.vitaprobe.html
*.probe.html
