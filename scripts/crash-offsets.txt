# Text-segment offsets from crash dumps (VitaSurf@1 + offset), one per
# line; the CI symbol map step prints the functions around each one.
#
# Build 62: data abort after pressing reload on en.wikipedia.org.
#   Resolved to libnsfb's scaled bitmap plotter, fixed by patch 0017.
0x6d2ba
# Build 86: data abort loading en.wikipedia.org, main thread, r0 = 0.
#   Resolved against build 86's own symbol map: the link register was in
#   node_get_elements_by_tag_name and the program counter had jumped
#   through a bogus pointer read past the end of a non-element node's
#   vtable. Fixed by checking the node type before every libdom element
#   call.
0xc32a2
0x932e
# Build 88: SIGABRT on the main thread about a second after a layout
#   rebuild started. Resolved against build 88's own symbol map: an
#   assertion in html_redraw, reached through browser_window_history_
#   update rendering the history thumbnail while the content had no box
#   tree. The rebuild runs to completion again rather than yielding.
0x35ff8e
# Build 110: data abort loading www.youtube.com, main thread, r0 = 0.
#   Module base was 0x81016000, so pc 0x810cdc9e is +0xb7c9e. Resolved
#   against build 110's own symbol map to __dom_dispatch_subtree_modified_
#   event + 0x42, whose only dereference of its first argument is
#   doc->_memo_domsubtreemodified, and r0 held that argument. libdom's
#   callers pass node->owner without checking it and a node created
#   outside any document has none. That guard was the wrong pointer: see
#   build 112.
0xb7c9e
# Build 112: the same crash again, with the build 110 guard in place, at
#   __dom_dispatch_subtree_modified_event + 0x46. Disassembled against
#   build 112's own ELF (the disasm workflow) rather than guessed at: the
#   faulting instruction is "ldr r3, [r4, #0]", the inlined vtable load
#   of dom_event_target_dispatch_event, and r4 -- the event target -- was
#   zero in the dump. It is the target that is null, not the document.
#   Fixed by guarding both in patch 0025.
0xb80aa

# Build 179: data abort opening youtube.com, main thread. Resolved
#   against build 179's own symbol map: the program counter was in
#   JS_CallInternal's frame setup, reached from js_function_apply, and
#   the core dump's thread record gave the answer -- a 256 KB stack of
#   which 0x3ffac was used. --gc-sections had dropped the
#   sceUserMainThreadStackSize definition, so the thread ran on the
#   runtime's default stack while the log printed the 4096 KB that had
#   been requested, and QuickJS's recursion guard was set to a megabyte,
#   larger than the whole stack, so it could never fire. Fixed by keeping
#   both runtime variables as link roots, measuring the stack the thread
#   actually got, and sizing the guard from that.
0xec8a8
0x133672
# Build 233: undefined instruction on the main thread after leaving
#   m.youtube.com for the history page and following a link back to it.
#   The log stops immediately after YouTube's 1.7 MB player script ran.
#   Resolved against build 233's own ELF (the disasm workflow): both
#   offsets are inside newlib's _kill_r, the program counter on its
#   closing "udf #255" and the link register on the instruction after
#   its call to sceKernelGetProcessId. _kill_r turns SIGINT and SIGTERM
#   into an exit, ignores two more, and traps on every other signal, so
#   landing there means raise() was called with something else -- which
#   is abort(), and on this build abort() is reached from a failed
#   assert(). It is not a corrupted function pointer: a Thumb
#   instruction sits at an even address, so an even program counter
#   says nothing, and the first reading of this dump was wrong.
#
#   Which assertion it was the dump cannot say, so the build now names
#   the failing expression, file and line in the log before it aborts.
0x39e94e
0x39e91b
# Build 286: data abort on the main thread while github.com's stylesheets
#   were converting. Resolved against build 286's own symbol map and the
#   disasm workflow: the program counter was in newlib's _free_r and the
#   caller css__stylesheet_selector_destroy, reached from parseSelector's
#   error path, and the pointer being freed lay in the data segment --
#   which is where a freed chunk's list link points, so the block had
#   been freed already. The native monkey under valgrind on the saved
#   GitHub sheets named it: parseGroupChain copies the details of an
#   :is() argument's last compound into the enclosing selector and then
#   destroys that compound, and a :has() detail's argument chain is a
#   pointer, not a reference, so ":is(.a .b:has(.c))" freed the chain
#   twice. Fixed by patch 0102, which hands the chain over.
0x3b3e12
0x8409f
0x89317
# Build 286: data abort on the main thread a few seconds into
#   m.youtube.com, r0 = 0. Resolved against the same symbol map: the
#   program counter was in fb_fill_scalar, called from fb_font_width
#   for the toolbar's status text. fbtk builds its font styles on the
#   stack and never set the families pointer, which the web font lookup
#   of patch 0096 reads once a page has registered a face -- YouTube's
#   Roboto was the first. Fixed by patch 0103.
0x69c34
0x6a031
# Build 294: assertion "parent" in box_add_child on the main thread,
#   about a second and a half after m.youtube.com's player script ran on
#   a second page. Resolved against build 294's ELF (the disasm
#   workflow): box_construct.c:959 in box_construct_element_after,
#   reached from next_node's climb back up the tree during a box
#   rebuild after script changes. An inline element with converted
#   children had no containing block: every ancestor with a box was
#   inline, or had none. Which element, the dump cannot say and the
#   snapshot of the watch page does not reach that state offline, so
#   patch 0106 logs the element's name and leaves its inline end out
#   rather than aborting.
0x1ed33
0x1ef5f
