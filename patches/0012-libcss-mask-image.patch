diff --git a/include/libcss/computed.h b/include/libcss/computed.h
index c048e64..f372d76 100644
--- a/include/libcss/computed.h
+++ b/include/libcss/computed.h
@@ -180,6 +180,10 @@ uint8_t css_computed_background_image(
 		const css_computed_style *style,
 		lwc_string **url);
 
+uint8_t css_computed_mask_image(
+		const css_computed_style *style,
+		lwc_string **url);
+
 uint8_t css_computed_color(
 		const css_computed_style *style,
 		css_color *color);
diff --git a/include/libcss/properties.h b/include/libcss/properties.h
index cb1f0ff..d5faefa 100644
--- a/include/libcss/properties.h
+++ b/include/libcss/properties.h
@@ -140,6 +140,7 @@ enum css_properties_e {
 	CSS_PROP_ORDER				= 0x07b,
 	CSS_PROP_FILL_OPACITY			= 0x07c,
 	CSS_PROP_STROKE_OPACITY			= 0x07d,
+	CSS_PROP_MASK_IMAGE			= 0x07e,
 
 	CSS_N_PROPERTIES
 };
@@ -193,6 +194,13 @@ enum css_background_image_e {
 	CSS_BACKGROUND_IMAGE_IMAGE		= 0x1
 };
 
+enum css_mask_image_e {
+	CSS_MASK_IMAGE_INHERIT			= 0x0,
+	/* Consult pointer in struct to determine which */
+	CSS_MASK_IMAGE_NONE			= 0x1,
+	CSS_MASK_IMAGE_IMAGE			= 0x1
+};
+
 enum css_background_position_e {
 	CSS_BACKGROUND_POSITION_INHERIT		= 0x0,
 	CSS_BACKGROUND_POSITION_SET		= 0x1
diff --git a/src/bytecode/opcodes.h b/src/bytecode/opcodes.h
index 44ab23d..5aee852 100644
--- a/src/bytecode/opcodes.h
+++ b/src/bytecode/opcodes.h
@@ -73,6 +73,11 @@ enum op_background_image {
 	BACKGROUND_IMAGE_NONE		= 0x0000
 };
 
+enum op_mask_image {
+	MASK_IMAGE_URI			= 0x0080,
+	MASK_IMAGE_NONE			= 0x0000
+};
+
 enum op_background_position {
 	BACKGROUND_POSITION_HORZ_SET	= 0x0080,
 	BACKGROUND_POSITION_HORZ_CENTER	= 0x0000,
diff --git a/src/parse/important.c b/src/parse/important.c
index 950fc6a..09c5b4e 100644
--- a/src/parse/important.c
+++ b/src/parse/important.c
@@ -113,6 +113,7 @@ void css__make_style_important(css_style *style)
 				break;
 
 			case CSS_PROP_BACKGROUND_IMAGE:
+			case CSS_PROP_MASK_IMAGE:
 			case CSS_PROP_CUE_AFTER:
 			case CSS_PROP_CUE_BEFORE:
 			case CSS_PROP_LIST_STYLE_IMAGE:
diff --git a/src/parse/properties/Makefile b/src/parse/properties/Makefile
index df84d49..2887e16 100644
--- a/src/parse/properties/Makefile
+++ b/src/parse/properties/Makefile
@@ -30,6 +30,7 @@ $(eval $(foreach PROP,$(AUTOGEN_PARSERS),$(call gen_prop_parser,$(PROP))))
 
 
 DIR_SOURCES :=				\
+	mask_image.c \
 	azimuth.c			\
 	background.c			\
 	background_position.c		\
diff --git a/src/parse/properties/mask_image.c b/src/parse/properties/mask_image.c
new file mode 100644
index 0000000..bd6ae50
--- /dev/null
+++ b/src/parse/properties/mask_image.c
@@ -0,0 +1,18 @@
+/*
+ * This file is part of LibCSS.
+ * Licensed under the MIT License,
+ *		  http://www.opensource.org/licenses/mit-license.php
+ *
+ * mask-image aliases (VitaSurf addition). The mask-image parser itself is
+ * generated from properties.gen; -webkit-mask-image, which stylesheets
+ * still ship alongside the unprefixed property, produces the same bytecode.
+ */
+
+#include "parse/properties/properties.h"
+
+css_error css__parse_webkit_mask_image(css_language *c,
+		const parserutils_vector *vector, int32_t *ctx,
+		css_style *result)
+{
+	return css__parse_mask_image(c, vector, ctx, result);
+}
diff --git a/src/parse/properties/properties.c b/src/parse/properties/properties.c
index 2cc849c..6411742 100644
--- a/src/parse/properties/properties.c
+++ b/src/parse/properties/properties.c
@@ -12,6 +12,7 @@
  */
 const css_prop_handler property_handlers[LAST_PROP + 1 - FIRST_PROP] =
 {
+	css__parse_webkit_mask_image,
 	css__parse_align_content,
 	css__parse_align_items,
 	css__parse_align_self,
@@ -103,6 +104,7 @@ const css_prop_handler property_handlers[LAST_PROP + 1 - FIRST_PROP] =
 	css__parse_margin_left,
 	css__parse_margin_right,
 	css__parse_margin_top,
+	css__parse_mask_image,
 	css__parse_max_height,
 	css__parse_max_width,
 	css__parse_min_height,
@@ -265,6 +267,7 @@ const uint32_t property_unit_mask[CSS_N_PROPERTIES] = {
 	[CSS_PROP_OPACITY]               = UNIT_MASK_OPACITY,
 	[CSS_PROP_FILL_OPACITY]          = UNIT_MASK_FILL_OPACITY,
 	[CSS_PROP_STROKE_OPACITY]        = UNIT_MASK_STROKE_OPACITY,
+	[CSS_PROP_MASK_IMAGE]            = UNIT_MASK_MASK_IMAGE,
 	[CSS_PROP_BREAK_AFTER]           = UNIT_MASK_BREAK_AFTER,
 	[CSS_PROP_BREAK_BEFORE]          = UNIT_MASK_BREAK_BEFORE,
 	[CSS_PROP_BREAK_INSIDE]          = UNIT_MASK_BREAK_INSIDE,
diff --git a/src/parse/properties/properties.gen b/src/parse/properties/properties.gen
index 3452f81..bb1549b 100644
--- a/src/parse/properties/properties.gen
+++ b/src/parse/properties/properties.gen
@@ -123,6 +123,8 @@ clear:CSS_PROP_CLEAR IDENT:( INHERIT: INITIAL: REVERT: UNSET: RIGHT:0,CLEAR_RIGH
 
 background_image:CSS_PROP_BACKGROUND_IMAGE IDENT:( INHERIT: INITIAL: REVERT: UNSET: NONE:0,BACKGROUND_IMAGE_NONE IDENT:) URI:BACKGROUND_IMAGE_URI
 
+mask_image:CSS_PROP_MASK_IMAGE IDENT:( INHERIT: INITIAL: REVERT: UNSET: NONE:0,MASK_IMAGE_NONE IDENT:) URI:MASK_IMAGE_URI
+
 list_style_image:CSS_PROP_LIST_STYLE_IMAGE IDENT:( INHERIT: INITIAL: REVERT: UNSET: NONE:0,LIST_STYLE_IMAGE_NONE IDENT:) URI:LIST_STYLE_IMAGE_URI
 
 list_style_position:CSS_PROP_LIST_STYLE_POSITION IDENT:( INHERIT: INITIAL: REVERT: UNSET: INSIDE:0,LIST_STYLE_POSITION_INSIDE OUTSIDE:0,LIST_STYLE_POSITION_OUTSIDE IDENT:)
diff --git a/src/parse/properties/properties.h b/src/parse/properties/properties.h
index 17b7f41..32ef244 100644
--- a/src/parse/properties/properties.h
+++ b/src/parse/properties/properties.h
@@ -46,6 +46,12 @@ css_error css__parse_background_color(css_language *c,
 css_error css__parse_background_image(css_language *c,
 		const parserutils_vector *vector, int32_t *ctx,
 		css_style *result);
+css_error css__parse_mask_image(css_language *c,
+		const parserutils_vector *vector, int32_t *ctx,
+		css_style *result);
+css_error css__parse_webkit_mask_image(css_language *c,
+		const parserutils_vector *vector, int32_t *ctx,
+		css_style *result);
 css_error css__parse_background_position(css_language *c,
 		const parserutils_vector *vector, int32_t *ctx,
 		css_style *result);
@@ -471,6 +477,7 @@ extern const uint32_t property_unit_mask[CSS_N_PROPERTIES];
 #define UNIT_MASK_BACKGROUND_ATTACHMENT (0)
 #define UNIT_MASK_BACKGROUND_COLOR      (0)
 #define UNIT_MASK_BACKGROUND_IMAGE      (0)
+#define UNIT_MASK_MASK_IMAGE            (0)
 #define UNIT_MASK_BACKGROUND_POSITION   (UNIT_LENGTH | UNIT_PCT)
 #define UNIT_MASK_BACKGROUND_REPEAT     (0)
 #define UNIT_MASK_BORDER_COLLAPSE       (0)
diff --git a/src/parse/propstrings.c b/src/parse/propstrings.c
index 2935f4e..a97f587 100644
--- a/src/parse/propstrings.c
+++ b/src/parse/propstrings.c
@@ -86,6 +86,7 @@ const stringmap_entry stringmap[LAST_KNOWN] = {
 	SMAP("before"),
 	SMAP("after"),
 
+	SMAP("-webkit-mask-image"),
 	SMAP("align-content"),
 	SMAP("align-items"),
 	SMAP("align-self"),
@@ -177,6 +178,7 @@ const stringmap_entry stringmap[LAST_KNOWN] = {
 	SMAP("margin-left"),
 	SMAP("margin-right"),
 	SMAP("margin-top"),
+	SMAP("mask-image"),
 	SMAP("max-height"),
 	SMAP("max-width"),
 	SMAP("min-height"),
diff --git a/src/parse/propstrings.h b/src/parse/propstrings.h
index ade2af2..1072ef2 100644
--- a/src/parse/propstrings.h
+++ b/src/parse/propstrings.h
@@ -38,7 +38,8 @@ enum {
 	/* Properties */
 	FIRST_PROP,
 
-	ALIGN_CONTENT = FIRST_PROP, ALIGN_ITEMS, ALIGN_SELF, AZIMUTH,
+	WEBKIT_MASK_IMAGE = FIRST_PROP, ALIGN_CONTENT, ALIGN_ITEMS, ALIGN_SELF,
+	AZIMUTH,
 	BACKGROUND, BACKGROUND_ATTACHMENT, BACKGROUND_COLOR, BACKGROUND_IMAGE,
 	BACKGROUND_POSITION, BACKGROUND_REPEAT, BORDER, BORDER_BOTTOM,
 	BORDER_BOTTOM_COLOR, BORDER_BOTTOM_STYLE, BORDER_BOTTOM_WIDTH,
@@ -57,7 +58,7 @@ enum {
 	FONT_VARIANT, FONT_WEIGHT, HEIGHT, JUSTIFY_CONTENT, LEFT,
 	LETTER_SPACING, LINE_HEIGHT, LIST_STYLE, LIST_STYLE_IMAGE,
 	LIST_STYLE_POSITION, LIST_STYLE_TYPE, MARGIN, MARGIN_BOTTOM,
-	MARGIN_LEFT, MARGIN_RIGHT, MARGIN_TOP, MAX_HEIGHT, MAX_WIDTH,
+	MARGIN_LEFT, MARGIN_RIGHT, MARGIN_TOP, MASK_IMAGE, MAX_HEIGHT, MAX_WIDTH,
 	MIN_HEIGHT, MIN_WIDTH, OPACITY, ORDER, ORPHANS, OUTLINE, OUTLINE_COLOR,
 	OUTLINE_STYLE, OUTLINE_WIDTH, OVERFLOW, OVERFLOW_X, OVERFLOW_Y, PADDING,
 	PADDING_BOTTOM, PADDING_LEFT, PADDING_RIGHT, PADDING_TOP,
diff --git a/src/select/autogenerated_computed.h b/src/select/autogenerated_computed.h
index 7ff5f8b..ccfece2 100644
--- a/src/select/autogenerated_computed.h
+++ b/src/select/autogenerated_computed.h
@@ -85,6 +85,7 @@ struct css_computed_style_i {
  * margin_left                      2 + 5           4
  * margin_right                     2 + 5           4
  * margin_top                       2 + 5           4
+ * mask_image                       1             sizeof(ptr)
  * max_height                       2 + 5           4
  * max_width                        2 + 5           4
  * min_height                       2 + 5           4
@@ -152,9 +153,9 @@ struct css_computed_style_i {
  * quotes                           1             sizeof(ptr)
  * 
  * ---                            ---             ---
- *                                464 bits        236 + 8sizeof(ptr) bytes
+ *                                465 bits        236 + 9sizeof(ptr) bytes
  *                                ===================
- *                                294 + 8sizeof(ptr) bytes
+ *                                295 + 9sizeof(ptr) bytes
  * 
  * Bit allocations:
  * 
@@ -209,10 +210,10 @@ struct css_computed_style_i {
  * overflow_y; overflow_x; justify_content; font_family; flex_direction; clear;
  * stroke_opacity
  * 
- * 14 bbaaqorplfeicuCk................
+ * 14 bbaaqorpmlfeicuCk...............
  * background_color; background_attachment; quotes; orphans; order; opacity;
- * list_style_image; flex_shrink; flex_grow; fill_opacity; counter_reset;
- * counter_increment; color; background_image
+ * mask_image; list_style_image; flex_shrink; flex_grow; fill_opacity;
+ * counter_reset; counter_increment; color; background_image
  */
 	uint32_t bits[15];
 
@@ -255,6 +256,7 @@ struct css_computed_style_i {
 	css_fixed margin_left;
 	css_fixed margin_right;
 	css_fixed margin_top;
+	lwc_string *mask_image;
 	css_fixed max_height;
 	css_fixed max_width;
 	css_fixed min_height;
diff --git a/src/select/autogenerated_destroy.inc b/src/select/autogenerated_destroy.inc
index 078ea10..bb88867 100644
--- a/src/select/autogenerated_destroy.inc
+++ b/src/select/autogenerated_destroy.inc
@@ -76,6 +76,7 @@
 /* set_margin_left(style, 0, 0, CSS_UNIT_PX); */
 /* set_margin_right(style, 0, 0, CSS_UNIT_PX); */
 /* set_margin_top(style, 0, 0, CSS_UNIT_PX); */
+/* set_mask_image(style, 0, 0, CSS_UNIT_PX); */
 /* set_max_height(style, 0, 0, CSS_UNIT_PX); */
 /* set_max_width(style, 0, 0, CSS_UNIT_PX); */
 /* set_min_height(style, 0, 0, CSS_UNIT_PX); */
diff --git a/src/select/autogenerated_propget.h b/src/select/autogenerated_propget.h
index 8ebc0c4..38391b3 100644
--- a/src/select/autogenerated_propget.h
+++ b/src/select/autogenerated_propget.h
@@ -107,8 +107,8 @@ static inline uint8_t get_background_color(const css_computed_style *style,
 #undef BACKGROUND_COLOR_MASK
 
 #define BACKGROUND_IMAGE_INDEX 14
-#define BACKGROUND_IMAGE_SHIFT 16
-#define BACKGROUND_IMAGE_MASK 0x10000
+#define BACKGROUND_IMAGE_SHIFT 15
+#define BACKGROUND_IMAGE_MASK 0x8000
 static inline uint8_t get_background_image(const css_computed_style *style,
 		lwc_string **string)
 {
@@ -656,8 +656,8 @@ static inline uint8_t get_clip(
 #undef CLIP_MASK
 
 #define COLOR_INDEX 14
-#define COLOR_SHIFT 17
-#define COLOR_MASK 0x20000
+#define COLOR_SHIFT 16
+#define COLOR_MASK 0x10000
 static inline uint8_t get_color(const css_computed_style *style, css_color
 		*color)
 {
@@ -871,8 +871,8 @@ static inline uint8_t get_content(const css_computed_style *style, const
 #undef CONTENT_MASK
 
 #define COUNTER_INCREMENT_INDEX 14
-#define COUNTER_INCREMENT_SHIFT 18
-#define COUNTER_INCREMENT_MASK 0x40000
+#define COUNTER_INCREMENT_SHIFT 17
+#define COUNTER_INCREMENT_MASK 0x20000
 static inline uint8_t get_counter_increment(const css_computed_style *style,
 		const css_computed_counter **counter_arr)
 {
@@ -892,8 +892,8 @@ static inline uint8_t get_counter_increment(const css_computed_style *style,
 #undef COUNTER_INCREMENT_MASK
 
 #define COUNTER_RESET_INDEX 14
-#define COUNTER_RESET_SHIFT 19
-#define COUNTER_RESET_MASK 0x80000
+#define COUNTER_RESET_SHIFT 18
+#define COUNTER_RESET_MASK 0x40000
 static inline uint8_t get_counter_reset(const css_computed_style *style, const
 		css_computed_counter **counter_arr)
 {
@@ -991,8 +991,8 @@ static inline uint8_t get_empty_cells(const css_computed_style *style)
 #undef EMPTY_CELLS_MASK
 
 #define FILL_OPACITY_INDEX 14
-#define FILL_OPACITY_SHIFT 20
-#define FILL_OPACITY_MASK 0x100000
+#define FILL_OPACITY_SHIFT 19
+#define FILL_OPACITY_MASK 0x80000
 static inline uint8_t get_fill_opacity(const css_computed_style *style,
 		css_fixed *fixed)
 {
@@ -1057,8 +1057,8 @@ static inline uint8_t get_flex_direction(const css_computed_style *style)
 #undef FLEX_DIRECTION_MASK
 
 #define FLEX_GROW_INDEX 14
-#define FLEX_GROW_SHIFT 21
-#define FLEX_GROW_MASK 0x200000
+#define FLEX_GROW_SHIFT 20
+#define FLEX_GROW_MASK 0x100000
 static inline uint8_t get_flex_grow(const css_computed_style *style, css_fixed
 		*fixed)
 {
@@ -1080,8 +1080,8 @@ static inline uint8_t get_flex_grow(const css_computed_style *style, css_fixed
 #undef FLEX_GROW_MASK
 
 #define FLEX_SHRINK_INDEX 14
-#define FLEX_SHRINK_SHIFT 22
-#define FLEX_SHRINK_MASK 0x400000
+#define FLEX_SHRINK_SHIFT 21
+#define FLEX_SHRINK_MASK 0x200000
 static inline uint8_t get_flex_shrink(const css_computed_style *style,
 		css_fixed *fixed)
 {
@@ -1361,8 +1361,8 @@ static inline uint8_t get_line_height(
 #undef LINE_HEIGHT_MASK
 
 #define LIST_STYLE_IMAGE_INDEX 14
-#define LIST_STYLE_IMAGE_SHIFT 23
-#define LIST_STYLE_IMAGE_MASK 0x800000
+#define LIST_STYLE_IMAGE_SHIFT 22
+#define LIST_STYLE_IMAGE_MASK 0x400000
 static inline uint8_t get_list_style_image(const css_computed_style *style,
 		lwc_string **string)
 {
@@ -1515,6 +1515,27 @@ static inline uint8_t get_margin_top(const css_computed_style *style, css_fixed
 #undef MARGIN_TOP_SHIFT
 #undef MARGIN_TOP_MASK
 
+#define MASK_IMAGE_INDEX 14
+#define MASK_IMAGE_SHIFT 23
+#define MASK_IMAGE_MASK 0x800000
+static inline uint8_t get_mask_image(const css_computed_style *style,
+		lwc_string **string)
+{
+	uint8_t type;
+	uint32_t bits = style->i.bits[MASK_IMAGE_INDEX];
+	bits &= MASK_IMAGE_MASK;
+	bits >>= MASK_IMAGE_SHIFT;
+
+	/* 1bit: t : type */
+	type = bits & 0x1;
+	*string = style->i.mask_image;
+
+	return type;
+}
+#undef MASK_IMAGE_INDEX
+#undef MASK_IMAGE_SHIFT
+#undef MASK_IMAGE_MASK
+
 #define MAX_HEIGHT_INDEX 5
 #define MAX_HEIGHT_SHIFT 11
 #define MAX_HEIGHT_MASK 0x3f800
diff --git a/src/select/autogenerated_propset.h b/src/select/autogenerated_propset.h
index adcea47..bc217a6 100644
--- a/src/select/autogenerated_propset.h
+++ b/src/select/autogenerated_propset.h
@@ -107,8 +107,8 @@ static inline css_error set_background_color(css_computed_style *style, uint8_t
 #undef BACKGROUND_COLOR_MASK
 
 #define BACKGROUND_IMAGE_INDEX 14
-#define BACKGROUND_IMAGE_SHIFT 16
-#define BACKGROUND_IMAGE_MASK 0x10000
+#define BACKGROUND_IMAGE_SHIFT 15
+#define BACKGROUND_IMAGE_MASK 0x8000
 
 static inline css_error set_background_image(css_computed_style *style, uint8_t
 		type, lwc_string *string)
@@ -641,8 +641,8 @@ static inline css_error set_clip(
 #undef CLIP_MASK
 
 #define COLOR_INDEX 14
-#define COLOR_SHIFT 17
-#define COLOR_MASK 0x20000
+#define COLOR_SHIFT 16
+#define COLOR_MASK 0x10000
 
 static inline css_error set_color(css_computed_style *style, uint8_t type,
 		css_color color)
@@ -904,8 +904,8 @@ static inline css_error set_content(
 #undef CONTENT_MASK
 
 #define COUNTER_INCREMENT_INDEX 14
-#define COUNTER_INCREMENT_SHIFT 18
-#define COUNTER_INCREMENT_MASK 0x40000
+#define COUNTER_INCREMENT_SHIFT 17
+#define COUNTER_INCREMENT_MASK 0x20000
 
 static inline css_error set_counter_increment(css_computed_style *style,
 		uint8_t type, css_computed_counter *counter_arr)
@@ -940,8 +940,8 @@ static inline css_error set_counter_increment(css_computed_style *style,
 #undef COUNTER_INCREMENT_MASK
 
 #define COUNTER_RESET_INDEX 14
-#define COUNTER_RESET_SHIFT 19
-#define COUNTER_RESET_MASK 0x80000
+#define COUNTER_RESET_SHIFT 18
+#define COUNTER_RESET_MASK 0x40000
 
 static inline css_error set_counter_reset(css_computed_style *style, uint8_t
 		type, css_computed_counter *counter_arr)
@@ -1066,8 +1066,8 @@ static inline css_error set_empty_cells(css_computed_style *style, uint8_t type)
 #undef EMPTY_CELLS_MASK
 
 #define FILL_OPACITY_INDEX 14
-#define FILL_OPACITY_SHIFT 20
-#define FILL_OPACITY_MASK 0x100000
+#define FILL_OPACITY_SHIFT 19
+#define FILL_OPACITY_MASK 0x80000
 
 static inline css_error set_fill_opacity(css_computed_style *style, uint8_t
 		type, css_fixed fixed)
@@ -1127,8 +1127,8 @@ static inline css_error set_flex_direction(css_computed_style *style, uint8_t
 #undef FLEX_DIRECTION_MASK
 
 #define FLEX_GROW_INDEX 14
-#define FLEX_GROW_SHIFT 21
-#define FLEX_GROW_MASK 0x200000
+#define FLEX_GROW_SHIFT 20
+#define FLEX_GROW_MASK 0x100000
 
 static inline css_error set_flex_grow(css_computed_style *style, uint8_t type,
 		css_fixed fixed)
@@ -1148,8 +1148,8 @@ static inline css_error set_flex_grow(css_computed_style *style, uint8_t type,
 #undef FLEX_GROW_MASK
 
 #define FLEX_SHRINK_INDEX 14
-#define FLEX_SHRINK_SHIFT 22
-#define FLEX_SHRINK_MASK 0x400000
+#define FLEX_SHRINK_SHIFT 21
+#define FLEX_SHRINK_MASK 0x200000
 
 static inline css_error set_flex_shrink(css_computed_style *style, uint8_t
 		type, css_fixed fixed)
@@ -1419,8 +1419,8 @@ static inline css_error set_line_height(css_computed_style *style, uint8_t
 #undef LINE_HEIGHT_MASK
 
 #define LIST_STYLE_IMAGE_INDEX 14
-#define LIST_STYLE_IMAGE_SHIFT 23
-#define LIST_STYLE_IMAGE_MASK 0x800000
+#define LIST_STYLE_IMAGE_SHIFT 22
+#define LIST_STYLE_IMAGE_MASK 0x400000
 
 static inline css_error set_list_style_image(css_computed_style *style, uint8_t
 		type, lwc_string *string)
@@ -1569,6 +1569,35 @@ static inline css_error set_margin_top(css_computed_style *style, uint8_t type,
 #undef MARGIN_TOP_SHIFT
 #undef MARGIN_TOP_MASK
 
+#define MASK_IMAGE_INDEX 14
+#define MASK_IMAGE_SHIFT 23
+#define MASK_IMAGE_MASK 0x800000
+
+static inline css_error set_mask_image(css_computed_style *style, uint8_t type,
+		lwc_string *string)
+{
+	uint32_t *bits = &style->i.bits[MASK_IMAGE_INDEX];
+
+	/* 1bit: t : type */
+	*bits = (*bits & ~MASK_IMAGE_MASK) | (((uint32_t)type & 0x1) <<
+			MASK_IMAGE_SHIFT);
+
+	lwc_string *old_string = style->i.mask_image;
+
+	if (string != NULL) {
+		style->i.mask_image = lwc_string_ref(string);
+	} else {
+		style->i.mask_image = NULL;
+	}
+
+	lwc_string_unref(old_string);
+
+	return CSS_OK;
+}
+#undef MASK_IMAGE_INDEX
+#undef MASK_IMAGE_SHIFT
+#undef MASK_IMAGE_MASK
+
 #define MAX_HEIGHT_INDEX 5
 #define MAX_HEIGHT_SHIFT 11
 #define MAX_HEIGHT_MASK 0x3f800
diff --git a/src/select/computed.c b/src/select/computed.c
index 60c3c7f..6163294 100644
--- a/src/select/computed.c
+++ b/src/select/computed.c
@@ -194,6 +194,7 @@ css_error css_computed_style_destroy(css_computed_style *style)
 
 	lwc_string_unref(style->i.list_style_image);
 	lwc_string_unref(style->i.background_image);
+	lwc_string_unref(style->i.mask_image);
 
 	if (style->calc != NULL)
 		css_calculator_unref(style->calc);
@@ -445,6 +446,12 @@ uint8_t css_computed_background_image(const css_computed_style *style,
 	return get_background_image(style, url);
 }
 
+uint8_t css_computed_mask_image(const css_computed_style *style,
+		lwc_string **url)
+{
+	return get_mask_image(style, url);
+}
+
 uint8_t css_computed_color(const css_computed_style *style,
 		css_color *color)
 {
diff --git a/src/select/dispatch.c b/src/select/dispatch.c
index cee9335..818e2df 100644
--- a/src/select/dispatch.c
+++ b/src/select/dispatch.c
@@ -522,5 +522,9 @@ struct prop_table prop_dispatch[CSS_N_PROPERTIES] = {
 	{
 		PROPERTY_FUNCS(stroke_opacity),
 		1,
+	},
+	{
+		PROPERTY_FUNCS(mask_image),
+		0,
 	}
 };
diff --git a/src/select/properties/Makefile b/src/select/properties/Makefile
index eee6cc3..6cd90b6 100644
--- a/src/select/properties/Makefile
+++ b/src/select/properties/Makefile
@@ -7,6 +7,7 @@ azimuth.c \
 background_attachment.c \
 background_color.c \
 background_image.c \
+mask_image.c \
 background_position.c \
 background_repeat.c \
 border_bottom_color.c \
diff --git a/src/select/properties/mask_image.c b/src/select/properties/mask_image.c
new file mode 100644
index 0000000..50c94a6
--- /dev/null
+++ b/src/select/properties/mask_image.c
@@ -0,0 +1,66 @@
+/*
+ * This file is part of LibCSS
+ * Licensed under the MIT License,
+ *		  http://www.opensource.org/licenses/mit-license.php
+ * Copyright 2009 John-Mark Bell <jmb@netsurf-browser.org>
+ */
+
+#include "bytecode/bytecode.h"
+#include "bytecode/opcodes.h"
+#include "select/propset.h"
+#include "select/propget.h"
+#include "utils/utils.h"
+
+#include "select/properties/properties.h"
+#include "select/properties/helpers.h"
+
+css_error css__cascade_mask_image(uint32_t opv, css_style *style,
+		css_select_state *state)
+{
+	return css__cascade_uri_none(opv, style, state, set_mask_image);
+}
+
+css_error css__set_mask_image_from_hint(const css_hint *hint,
+		css_computed_style *style)
+{
+	css_error error;
+
+	error = set_mask_image(style, hint->status, hint->data.string);
+
+	lwc_string_unref(hint->data.string);
+
+	return error;
+}
+
+css_error css__initial_mask_image(css_select_state *state)
+{
+	return set_mask_image(state->computed,
+			CSS_MASK_IMAGE_NONE, NULL);
+}
+
+css_error css__copy_mask_image(
+		const css_computed_style *from,
+		css_computed_style *to)
+{
+	lwc_string *url;
+	uint8_t type = get_mask_image(from, &url);
+
+	if (from == to) {
+		return CSS_OK;
+	}
+
+	return set_mask_image(to, type, url);
+}
+
+css_error css__compose_mask_image(const css_computed_style *parent,
+		const css_computed_style *child,
+		css_computed_style *result)
+{
+	lwc_string *url;
+	uint8_t type = get_mask_image(child, &url);
+
+	return css__copy_mask_image(
+			type == CSS_MASK_IMAGE_INHERIT ? parent : child,
+			result);
+}
+
diff --git a/src/select/properties/properties.h b/src/select/properties/properties.h
index cb0b213..23ad1f3 100644
--- a/src/select/properties/properties.h
+++ b/src/select/properties/properties.h
@@ -29,6 +29,7 @@ PROPERTY_FUNCS(azimuth);
 PROPERTY_FUNCS(background_attachment);
 PROPERTY_FUNCS(background_color);
 PROPERTY_FUNCS(background_image);
+PROPERTY_FUNCS(mask_image);
 PROPERTY_FUNCS(background_position);
 PROPERTY_FUNCS(background_repeat);
 PROPERTY_FUNCS(border_collapse);
diff --git a/src/select/select_config.py b/src/select/select_config.py
index a4ba423..9d59a18 100644
--- a/src/select/select_config.py
+++ b/src/select/select_config.py
@@ -64,6 +64,7 @@ style = {
     # Style group, with additional value
     ('background_color', 2, 'color'),
     ('background_image', 1, 'string'),
+    ('mask_image', 1, 'string'),
     ('background_position', 1, (('length',), ('length',)),
         'CSS_BACKGROUND_POSITION_SET'),
     ('border_top_color', 2, 'color'),
