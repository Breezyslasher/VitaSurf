diff --git a/content/handlers/image/image_cache.c b/content/handlers/image/image_cache.c
index 44c7b3102..96155388b 100644
--- a/content/handlers/image/image_cache.c
+++ b/content/handlers/image/image_cache.c
@@ -1213,6 +1213,12 @@ static void image_cache_decode(struct image_cache_entry_s *centry)
 #define DECODER_QUEUE_MAX 64
 /* the thread's stack: the decoders' own state is on the heap */
 #define DECODER_STACK_SIZE (256 * 1024)
+/*
+ * Decode threads at most. Build 442 spent 315-420 ms on each progressive
+ * poster JPEG with one thread, so a screenful took seconds to fill; two
+ * decode two at once, each on a core of its own (VitaSurf).
+ */
+#define DECODER_THREADS_MAX 2
 
 struct decode_job {
 	struct decode_job *next;
@@ -1227,20 +1233,23 @@ struct decode_job {
 static pthread_mutex_t decoder_lock = PTHREAD_MUTEX_INITIALIZER;
 static pthread_cond_t decoder_wake = PTHREAD_COND_INITIALIZER;
 static pthread_cond_t decoder_idle = PTHREAD_COND_INITIALIZER;
-static pthread_t decoder_thread;
+static pthread_t decoder_thread[DECODER_THREADS_MAX];
+static int decoder_threads;
 static bool decoder_on;
 static bool decoder_quit;
 static struct decode_job *decoder_head, *decoder_tail;
 static unsigned int decoder_waiting;
-static struct decode_job *decoder_running;
+/* the job each thread is decoding, NULL when it waits */
+static struct decode_job *decoder_running[DECODER_THREADS_MAX];
 static struct decode_job *decoder_done;
-static void (*decoder_started)(void);
+static void (*decoder_started)(int index);
 
 static void *image_cache_decoder_main(void *arg)
 {
-	(void)arg;
+	int index = (int)(intptr_t)arg;
+
 	if (decoder_started != NULL) {
-		decoder_started();
+		decoder_started(index);
 	}
 	pthread_mutex_lock(&decoder_lock);
 	while (!decoder_quit) {
@@ -1257,7 +1266,7 @@ static void *image_cache_decoder_main(void *arg)
 			decoder_tail = NULL;
 		}
 		decoder_waiting--;
-		decoder_running = job;
+		decoder_running[index] = job;
 		pthread_mutex_unlock(&decoder_lock);
 
 		t0 = vitasurf_clock_us();
@@ -1265,7 +1274,7 @@ static void *image_cache_decoder_main(void *arg)
 		job->us = vitasurf_clock_us() - t0;
 
 		pthread_mutex_lock(&decoder_lock);
-		decoder_running = NULL;
+		decoder_running[index] = NULL;
 		job->next = decoder_done;
 		decoder_done = job;
 		pthread_cond_broadcast(&decoder_idle);
@@ -1275,31 +1284,41 @@ static void *image_cache_decoder_main(void *arg)
 }
 
 /* exported interface documented in image_cache.h */
-bool image_cache_start_decoder(void (*started)(void))
+int image_cache_start_decoder(int threads, void (*started)(int index))
 {
 	pthread_attr_t attr;
-	bool ok;
+	int i;
 
 	if (decoder_on) {
-		return true;
+		return decoder_threads;
+	}
+	if (threads > DECODER_THREADS_MAX) {
+		threads = DECODER_THREADS_MAX;
 	}
 	decoder_started = started;
 	decoder_quit = false;
-	if (pthread_attr_init(&attr) != 0) {
-		return false;
+	if (threads < 1 || pthread_attr_init(&attr) != 0) {
+		return 0;
 	}
 	pthread_attr_setstacksize(&attr, DECODER_STACK_SIZE);
-	ok = pthread_create(&decoder_thread, &attr,
-			    image_cache_decoder_main, NULL) == 0;
+	for (i = 0; i < threads; i++) {
+		if (pthread_create(&decoder_thread[i], &attr,
+				   image_cache_decoder_main,
+				   (void *)(intptr_t)i) != 0) {
+			break;
+		}
+	}
 	pthread_attr_destroy(&attr);
-	decoder_on = ok;
-	return ok;
+	decoder_threads = i;
+	decoder_on = i > 0;
+	return i;
 }
 
-/** Stop the thread and throw away what it has not handed over. */
+/** Stop the threads and throw away what they have not handed over. */
 static void image_cache_decoder_stop(void)
 {
 	struct decode_job *job;
+	int i;
 
 	if (!decoder_on) {
 		return;
@@ -1308,7 +1327,10 @@ static void image_cache_decoder_stop(void)
 	decoder_quit = true;
 	pthread_cond_broadcast(&decoder_wake);
 	pthread_mutex_unlock(&decoder_lock);
-	pthread_join(decoder_thread, NULL);
+	for (i = 0; i < decoder_threads; i++) {
+		pthread_join(decoder_thread[i], NULL);
+	}
+	decoder_threads = 0;
 	decoder_on = false;
 
 	while ((job = decoder_head) != NULL) {
@@ -1410,7 +1432,18 @@ static void image_cache_decoder_forget(struct image_cache_entry_s *centry)
 			break;
 		}
 	}
-	while (decoder_running != NULL && decoder_running->centry == centry) {
+	for (;;) {
+		int i;
+
+		for (i = 0; i < decoder_threads; i++) {
+			if (decoder_running[i] != NULL &&
+			    decoder_running[i]->centry == centry) {
+				break;
+			}
+		}
+		if (i == decoder_threads) {
+			break;
+		}
 		pthread_cond_wait(&decoder_idle, &decoder_lock);
 	}
 	for (pp = &decoder_done; *pp != NULL; pp = &(*pp)->next) {
@@ -1522,8 +1555,14 @@ bool image_cache_decoder_busy(void)
 		return false;
 	}
 	pthread_mutex_lock(&decoder_lock);
-	busy = decoder_head != NULL || decoder_running != NULL ||
-		decoder_done != NULL;
+	busy = decoder_head != NULL || decoder_done != NULL;
+	{
+		int i;
+
+		for (i = 0; i < decoder_threads; i++) {
+			busy = busy || decoder_running[i] != NULL;
+		}
+	}
 	pthread_mutex_unlock(&decoder_lock);
 	return busy;
 }
diff --git a/content/handlers/image/image_cache.h b/content/handlers/image/image_cache.h
index 237c52e51..1ccf4cf40 100644
--- a/content/handlers/image/image_cache.h
+++ b/content/handlers/image/image_cache.h
@@ -263,14 +263,15 @@ int image_cache_take_deferred(struct rect *out, const void **key, int max);
 bool image_cache_ready(const void *key);
 
 /**
- * Start the decode thread (VitaSurf). Until it runs, every decode is on
- * the calling thread, paced by image_cache_frame().
+ * Start the decode threads (VitaSurf). Until they run, every decode is
+ * on the calling thread, paced by image_cache_frame().
  *
- * \param started Called on the thread as it starts, to set its core
- *                and priority; may be NULL.
- * \return true if the thread is running.
+ * \param threads How many to start (at most 2).
+ * \param started Called on each thread as it starts, with its index
+ *                from 0, to set its core and priority; may be NULL.
+ * \return how many are running.
  */
-bool image_cache_start_decoder(void (*started)(void));
+int image_cache_start_decoder(int threads, void (*started)(int index));
 
 /** Whether the decode thread has work queued, running or to hand over. */
 bool image_cache_decoder_busy(void);
diff --git a/frontends/framebuffer/gui.c b/frontends/framebuffer/gui.c
index 4033dbfdc..2ebf753c6 100644
--- a/frontends/framebuffer/gui.c
+++ b/frontends/framebuffer/gui.c
@@ -2816,15 +2816,18 @@ main(int argc, char** argv)
 		fb_warn_user("Errorcode:", messages_get_errorcode(ret));
 	} else {
 #ifdef __vita__
+		int decoders;
+
 		/* large images decode beside the page (VitaSurf) */
 		if (!vita_decode_thread_wanted()) {
 			vita_log("images: decoding stays on the main thread "
 				 "(flag file %s)",
 				 VITASURF_NO_DECODE_THREAD_FLAG);
-		} else if (image_cache_start_decoder(
-				   vita_decode_thread_started)) {
+		} else if ((decoders = image_cache_start_decoder(2,
+				   vita_decode_thread_started)) > 0) {
 			vita_log("images: large JPEG, PNG and WebP images "
-				 "decode on their own thread");
+				 "decode on %d threads of their own",
+				 decoders);
 		} else {
 			vita_log("images: the decode thread could not be "
 				 "started; decoding stays on the main thread");
