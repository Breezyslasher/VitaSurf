diff --git a/frontends/framebuffer/gui.c b/frontends/framebuffer/gui.c
index f54dd6034..c38da02ee 100644
--- a/frontends/framebuffer/gui.c
+++ b/frontends/framebuffer/gui.c
@@ -54,6 +54,10 @@
 #include "framebuffer/findfile.h"
 #include "framebuffer/image_data.h"
 #include "framebuffer/font.h"
+
+#ifdef __vita__
+#include "vita_platform.h"
+#endif
 #include "framebuffer/clipboard.h"
 #include "framebuffer/fetch.h"
 #include "framebuffer/bitmap.h"
@@ -2095,6 +2099,10 @@ gui_window_stop_throbber(struct gui_window *gw)
 
 	fb_update_back_forward(gw);
 
+#ifdef __vita__
+	/* VitaSurf: memory is the main constraint, log it after each load */
+	vita_log_memory("page load finished");
+#endif
 }
 
 static void
@@ -2231,6 +2239,10 @@ main(int argc, char** argv)
 		.utf8 = framebuffer_utf8_table,
 		.bitmap = framebuffer_bitmap_table,
 		.layout = framebuffer_layout_table,
+#ifdef __vita__
+		.file = vita_file_table,
+		.download = vita_download_table,
+#endif
 	};
 
         ret = netsurf_register(&framebuffer_table);
@@ -2274,6 +2286,11 @@ main(int argc, char** argv)
 		die("NetSurf failed to initialise");
 	}
 
+#ifdef __vita__
+	/* VitaSurf: the image handlers have registered by now */
+	vita_log_image_decoders();
+#endif
+
 	/* Override, since we have no support for non-core SELECT menu */
 	nsoption_set_bool(core_select_menu, true);
 
